// test.swf - Programme de test
import "math" from "stdlib";

var x = 10;
var y = 20.5;
var z = x + y;

print("x = " + x);
print("y = " + y);
print("z = x + y = " + z);

// Utiliser une constante du package
print("PI from math package = " + str(PI));

dbvar;

main() {
    print("Main function executed");
    return 0;
}
