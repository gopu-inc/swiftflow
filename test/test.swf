// test_final.swf
// Test d'import
import "math" from "stdlib";

// Variables de base
var x = 10;
var y = 20.5;
var z = x + y;

print("x = " + x);
print("y = " + y);
print("z = x + y = " + z);

// Test de PI si l'import fonctionne
if (PI) {
    print("PI = " + PI);
} else {
    print("PI not found (import issue)");
}

// Test des différents types
net net_var = 42;
clog clog_var = 3.14159;
dos dos_var = 1000;
sel sel_var = 64;

print("\nDifferent types:");
print("net: " + net_var);
print("clog: " + clog_var);
print("dos: " + dos_var);
print("sel: " + sel_var);

// Test de contrôle de flux
print("\nControl flow:");
if (x > 5) {
    print("x > 5 is true");
}

var counter = 0;
while (counter < 3) {
    print("While loop: counter = " + counter);
    counter = counter + 1;
}

// Test d'expressions
var a = 15;
var b = 3;
print("\nExpressions:");
print("a + b = " + (a + b));
print("a - b = " + (a - b));
print("a * b = " + (a * b));
print("a / b = " + (a / b));

// Afficher la table des variables
print("\nVariable table:");
dbvar;
