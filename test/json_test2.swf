
print("Testing JSON package...")

// Simple JSON string
var json_str = '{"name": "SwiftFlow", "version": 1.0, "features": ["fast", "simple", "powerful"]}'

// Parse JSON
var data = __json_parse__(json_str)
print("Parsed data type: " + typeof(data))

// Stringify back
var json_str2 = __json_stringify__(data)
print("Stringified: " + json_str2)

// Create array and convert to JSON
var arr = [1, 2, 3, "hello", true, null]
var arr_json = __json_stringify__(arr)
print("Array JSON: " + arr_json)

print("JSON test complete!")
