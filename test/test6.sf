// Test complet avec toutes les fonctionnalités

// Variables avec différents types
var x = 10;
net y = 20.5;
clog message = "Hello SwiftFlow!";
dos big_data = 1000.75;

// Fonction principale
sel main() {
    // Afficher les valeurs
    print(x);
    print(y);
    print(message);
    print(big_data);
    
    // Afficher les tailles
    print("Size of x: ", size(x));
    print("Size of y: ", size(y));
    print("Size of message: ", size(message));
    print("Size of big_data: ", size(big_data));
    
    // Structure de contrôle if/else
    if (x > 5) {
        print("x is greater than 5");
    } else {
        print("x is less than or equal to 5");
    }
    
    // Boucle while
    var counter = 0;
    while (counter < 3) {
        print("Counter: ", counter);
        counter = counter + 1;
    }
    
    // Afficher la table des variables
    dbvar;
    
    return 0;
}
