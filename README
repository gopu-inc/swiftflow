SWIFTVELOX - Langage inspiré de Go/Zig en C pur

STRUCTURE:
├── src/           - Code source du compilateur
│   ├── main.c     - Point d'entrée
│   ├── compiler.c - Compilateur principal
│   └── runtime.c  - Runtime minimal
├── examples/      - Exemples de code
├── Makefile       - Construction
└── start.sh       - Script de démarrage

COMPILATION:
  make                    # Construire le compilateur
  ./swiftvelox version    # Vérifier la version

UTILISATION:
  ./swiftvelox build fichier.svx   # Compiler en C
  ./swiftvelox run fichier.svx     # Compiler + exécuter

EXEMPLE:
  // hello.svx
  fn main() {
      swget("Hello SwiftVelox!")
  }

  ./swiftvelox run hello.svx

CARACTÉRISTIQUES:
  - 100% C standard (C99)
  - Pas de dépendances externes
  - Compilation vers C pur
  - Runtime minimal inclus
  - Fonctionne sur Alpine Linux
